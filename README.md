# Требования

- Node.js >= 22.13.0 и доступна в PATH
- npm >= 10.9.0 и доступен в PATH
- Git установлен и доступен в PATH

# Запуск проекта

```bash
git clone <repository-url>
cd tank-list
npm install
npm run dev
```
